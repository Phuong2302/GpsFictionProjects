<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
android:id="@+id/ContentAdmin"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:orientation="vertical"
    android:gravity="center"
    android:background="@color/black"
    android:padding="@dimen/buttonsPadding"
    >
<RelativeLayout
    android:layout_gravity="center_horizontal"
    android:layout_margin="@dimen/buttonsPadding"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

<Spinner
    android:id="@+id/LanguageListSpinner"
    android:spinnerMode="dropdown"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@android:drawable/btn_dropdown"
    android:dropDownWidth="fill_parent"

    />
</RelativeLayout>

<Button
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal"
    android:padding="@dimen/buttonsPadding"
    android:textSize="@dimen/adminTextSize"
    android:layout_margin="@dimen/buttonsPadding"
    android:background="@drawable/buttonstate"
    android:text="Change Admin Password"
    android:onClick="changeAdminPassword"
    />


<ToggleButton
    android:text="Reset Games (new Player)"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal"
    android:padding="@dimen/buttonsPadding"
    android:layout_margin="@dimen/buttonsPadding"
    android:background="@drawable/buttonstate"
    android:textColor="@drawable/textbuttonstate"
    android:textSize="@dimen/adminTextSize"
    />


<Button
    android:text="Start Games"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal"
    android:padding="@dimen/buttonsPadding"
    android:layout_margin="@dimen/buttonsPadding"
    android:textSize="@dimen/adminTextSize"
    android:background="@drawable/buttonstate"
    android:onClick="startGames"
    />
    <TextView
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:id="@+id/mytext"
        android:padding="@dimen/buttonsPadding"
        android:textSize="@dimen/adminTextSize"
        android:text="temp"
        android:background="@color/white"
        />
</LinearLayout>
